# Development dependencies are declared in pyproject.toml and locked in uv.lock.
# torch comes from the PyTorch CPU index when installed through uv; plain pip
# resolves the default build.
-e .
httpx==0.28.1
mypy==2.3.1
pip-audit==2.10.1
pypdf==6.19.0
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
requests==2.34.2
ruff==0.16.8
sentence-transformers==6.1.0
torch==2.14.0
