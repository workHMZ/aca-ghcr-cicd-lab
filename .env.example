# Azure AI Search Settings (Required)
# Get these from Azure Portal -> Your Search Service -> Keys
AZURE_SEARCH_ENDPOINT=https://your-search-service.search.windows.net
AZURE_SEARCH_API_KEY=your-api-key-here
AZURE_SEARCH_INDEX_NAME=rag-documents

# Note: Azure OpenAI settings are NOT required for this project
# We use local sentence-transformers (all-MiniLM-L6-v2) for embedding
# This saves costs on Azure Student Subscription!
